# Session Handoff — 2026-04-23

> **Purpose.** Capture enough state that after a context compaction (or a fresh session) work can resume without re-deriving decisions. Read this first, then the spec, then the plan, then the retro.

---

## Current state in one paragraph

Wave 0 of the Dalio-Framework project is **complete through Task 4** of `docs/superpowers/plans/2026-04-23-wave0-pilot.md`. Two pilot research agents ran successfully against the deterministic prompt in `research/_prompt_template.md`, producing `research/01_economic_machine.md` (subsection 1.1) and `research/09_all_weather.md` (subsection 2.2). A retrospective in `research/_wave0_retro.md` initially reported VERDICT-S, but the advisor catch flipped it to **VERDICT-P** when a shared attribution-discipline slip was found across both pilots (operational thresholds appearing in § 5 / § 6 without `> **NON-DALIO**` or `> **DERIVED**` markers at the point of use, even though § 10 acknowledged them). Three refinements are proposed (#3 registry fix, #5 new Hard Rule R10 + acceptance criterion R7b) that BLOCK Wave 1; two more (#1 worked-example data policy, #2 web-source citation style) are cheap wins; #4 title standardization is optional. **The pilots are NOT being re-spawned** — they'll be patched in place during consolidation. Wave 1 (subsections 1.2, 1.3, 1.4, 1.5) is blocked on user review / approval of the refinements.

## User's last instruction

> "finish your current task, then prepare for compaction thoroughly, and wait for me"

So: do NOT apply refinements autonomously, do NOT launch Wave 1. **Wait for user** to review and direct.

## What to do on resume

**Step 1:** Present Wave 0 retro findings to the user. In chat, summarize:
- Pass rate and VERDICT-P flip (with advisor-caught details — see retro § 2.5)
- The 5 proposed refinements (retro § 3) and which are blocking
- Ask user: approve R3 + R5 (blocking) and R1 + R2 (cheap wins)? skip R4? anything else?

Use `AskUserQuestion` tool.

**Step 2 (after approval):** Apply approved refinements:
- `research/_prompt_template.md`:
  - Fix IN bullet for 2.2 All-Weather (replace "15% IL bonds" with "15% intermediate nominal Treasuries ..." — see retro § 3 #3 for exact text)
  - Add Hard Rule R10 (point-of-use attribution for derived thresholds — see retro § 3 #5 for exact text)
  - If R1 approved: add the worked-example data-policy note to § 7 schema
  - If R2 approved: add the web-source citation style note to § 2 schema
- `research/_acceptance_criteria.md`:
  - Add R7b (point-of-use spot check — see retro § 3 #5 for exact text)
- `docs/superpowers/specs/2026-04-23-dalio-framework-design.md` § 5:
  - Same IL-bonds -> intermediate-Treasuries fix for consistency
- Commit + push each group of edits with a clear message

**Step 3:** Launch Wave 1 (subsections 1.2, 1.3, 1.4, 1.5) as 4 parallel general-purpose agents in a single message. Each agent gets the full filled prompt (template + substituted slots + FINAL STEP save-to-file instruction). Expect ~15–30 min wall-clock.

**Step 4:** Repeat the evaluate + retro loop on Wave 1's four reports. Apply any further refinements. Launch Wave 2 (1.6, 1.7, 2.1, 2.2... but 2.2 already piloted — so Wave 2 = 1.6, 1.7, 2.1, 2.3 or similar — re-check plan).

Actually on re-read of plan § 6: Wave 1 batches are 1.2/1.3/1.4/1.5 then 1.6/1.7/2.1/2.2 then 2.3/2.4/2.5. But 2.2 is already done, so its wave should skip it — Wave 2 becomes 1.6/1.7/2.1 (three), Wave 3 becomes 2.3/2.4/2.5 (three). Adjust plan on resume.

## Key file paths

| File | Role |
|---|---|
| `README.md` | Placeholder (will be replaced at project end by master framework doc) |
| `LICENSE` | MIT |
| `.gitignore` | OS/editor/secrets exclusions |
| `dalio_market_principles_syllabus.md` | Reference: the actual DMP Online syllabus |
| `docs/superpowers/specs/2026-04-23-dalio-framework-design.md` | **THE design contract** — 17 sections. Start here for decisions. |
| `docs/superpowers/plans/2026-04-23-wave0-pilot.md` | Wave 0 implementation plan, 6 tasks (5 now complete, 2 pending — really tasks 5 & 6) |
| `docs/superpowers/HANDOFF_2026-04-23.md` | This file |
| `research/_prompt_template.md` | Source of truth for all 12 research agents — prompt + subsection registry |
| `research/_acceptance_criteria.md` | 19-item pass/fail checklist |
| `research/_wave0_retro.md` | Wave 0 findings, verdict, refinements |
| `research/01_economic_machine.md` | Pilot A output (subsection 1.1) — substance passes, attribution patch needed |
| `research/09_all_weather.md` | Pilot B output (subsection 2.2) — substance passes, attribution patch needed |

## Locked decisions (from spec § 3)

| # | Decision | Value |
|---|---|---|
| D1 | Corpus scope | Dalio's investment + macro (no Life & Work) |
| D2 | Subsection count | 12 (7 macro + 5 investment) |
| D3 | Agents per subsection | Exactly 1 |
| D4 | Output schema strictness | Strict — 10 required sections |
| D5 | Per-report depth | "Concise + complete; no point lost; word count is secondary" (user-updated) |
| D6 | Example-number source | Agent specifies sources; JS/Excel fills real numbers |
| D7 | Artifacts shipped | 3 — `README.md`, `dalio_dashboard.html`, `dalio_model.xlsx` |
| D8 | Python role | Internal only, NOT shipped |
| D9 | In-browser runtime | Pure JavaScript (no Pyodide) |
| D10 | Dashboard layout | By user workflow (Regime → Portfolio → Stress → Log → Reference) |
| D11 | Excel data engine | Power Query |
| D12 | Chart library | ECharts via CDN |
| D13 | Visual language | Claude-editorial × Apple-elegant × black + elegant green (see spec § 13) |
| D14 | Execution cadence | Pilot → review → batches of 4 |
| D15 | Dashboard design | Iterative multi-pass phase (see spec § 16) |

## Decisions made during the session that may NOT be in the spec yet

- **User directive:** word count is less important than material relevance and accuracy, and "no point should be lost." This updates D5. The acceptance criteria's S2 (word count 2000–3000) should be demoted to informational, not pass/fail. Add to `_acceptance_criteria.md` on resume if not already.
- **User directive:** no library PDFs (no uOttawa Omni pull). Spec § 7 already reflects this.
- **User directive:** public repo, so only publicly-verifiable sources; attribution must distinguish Dalio vs NON-DALIO. Spec § 2 + § 7 reflect this.
- **Advisor cadence:** batch to end-of-plan + post-pilot. Not a durable rule — just for this project.

## Git state

- Repo: https://github.com/Siriko404/RayDalioAttitude (public)
- Branch: `main`
- Most recent commits (latest first):
  - `fffd493` fix(retro): flip verdict to VERDICT-P after advisor catch + add R5/R7b
  - `e283ba4` docs(research): add Wave 0 retrospective with VERDICT-S
  - `a2e18ad` feat(research): add Wave 0 pilot reports (1.1 Economic Machine, 2.2 All-Weather)
  - `ce701b0` feat(research): add objective acceptance criteria checklist
  - `9c34619` feat(research): add deterministic prompt template + 12-subsection registry
  - `0ef8ccd` refactor(plan): apply advisor feedback to Wave 0 plan
  - `b0c8499` feat(plan): add Wave 0 dual-pilot plan + spec updates
  - `8a16d76` chore: initial commit — project scaffold + design spec + syllabus

## Open items the user should know about

1. **Wave 1 is blocked** on user approval of R3, R5 (blocking) and R1, R2 (cheap wins) — see retro § 3.
2. **Pilots 1.1 and 2.2 have attribution-markup gaps** that need in-place patches during consolidation (not re-spawn). See retro § 2 "Shared slip" for the exact lines to patch.
3. **Spec § 5 IL-bonds line** still says "15% IL bonds"; needs fix when prompt template is fixed (R3).
4. **Plan's wave map** needs to skip 2.2 in Wave 2 (since it's already piloted). Adjust on resume.
