# Dalio Framework — Compaction Handoff (2026-04-24)

> Supersedes `HANDOFF_2026-04-23.md`. Read this first on resume.

## TL;DR — Where We Are

- **All 12 research reports written + initially audited.** Layout has shifted: audits now live in `chatgpt_audit_kit/`, not `research/`.
- **Audit approach pivoted mid-session from Sonnet to ChatGPT.** ChatGPT does all audits going forward. Re-spawns/rewrites stay on Sonnet (for now).
- **Three issues require attention on resume** (see "Open Items" below).
- **No background agents currently running.** Last in-flight (1.7 re-spawn `ade534`) landed and is committed.

## Repo State (Verified Clean At Compaction)

```
HEAD = 05c1dea  checkpoint: 1.7 re-spawn landed + 12 ChatGPT audits in audit-kit dir
```

Recent commits (most recent first):
```
05c1dea  checkpoint: 1.7 re-spawn landed + 12 ChatGPT audits in audit-kit dir
1561e9c  fix(audit-kit): file-output rule airtight — EVERYTHING in the .md
f905ace  fix(audit-kit): file-output + softened network-failure framing
da44ab9  fix(audit-kit): advisor surgical fixes for first ChatGPT use
7353572  feat(audit-kit): collapse to pure 2-file upload workflow
70b0f9c  fix(audit-kit): advisor-flagged gaps before first ChatGPT use
f02e8cb  feat(audit-kit): single-file ChatGPT audit prompt (Layer 1)
44e376c  audit(1.7): REJECT-re-spawn — 1 CRITICAL/MAJOR + 3 MAJOR + 4 MINOR
bb68618  research(1.6): re-spawn — all audit findings dispatched
1ba300d  audit(1.6): REJECT-re-spawn — 1 CRITICAL + 4 MAJOR + 5 MINOR
1d42344  audit(2.5): apply 4 MINOR patches + record audit — PASS-with-patches
098f24d  audit(2.4): apply 3 patches + record audit — PASS-with-patches
871b41e  audit(2.3): apply 7 patches + record audit — PASS-with-patches
b73add8  feat(research): 2.5 Stress-Testing & Scenario Analysis
1a41048  feat(research): 2.4 Risk Parity & Leverage
5ee3b1e  feat(research): 2.3 Alpha Generation & Portable Alpha
```

## File Inventory

### Research reports (`research/`)
```
01_economic_machine.md            Wave 0 patched-in-place; ChatGPT re-audited (REJECT, see _audit_01)
02_short_term_debt_cycle.md       Wave 1 re-spawn
03_long_term_debt_cycle.md        Wave 1 re-spawn
04_deleveragings.md               Wave 1 re-spawn
05_paradigm_shifts.md             Wave 1 re-spawn
06_changing_world_order.md        Wave 2 re-spawned (bb68618 dispatched all 10 audit findings)
07_inflation_currency.md          Wave 2 1.7 RE-SPAWN LANDED 2026-04-24, but 3811w — OVER 3000 CAP
08_template_for_investing.md      Wave 2 (no Sonnet audit ran; ChatGPT audit done — _audit_08)
09_all_weather.md                 Wave 0 re-spawn
10_alpha_portable_alpha.md        Wave 3 + Sonnet audit patches (871b41e)
11_risk_parity_leverage.md        Wave 3 + Sonnet audit patches (098f24d)
12_stress_testing.md              Wave 3 + Sonnet audit patches (1d42344)
```

### Scaffolding (`research/`)
- `_prompt_template.md` — research prompt + R1-R14 + scope registry
- `_redteam_prompt.md` — Sonnet audit prompt (legacy; replaced by ChatGPT kit)
- `_acceptance_criteria.md` — 21-item checklist
- `_wave0_retro.md`, `_wave1_retro.md` — retros
- `_tmp_*.txt` — agent scratch (gitignored)

### ChatGPT audit kit (`chatgpt_audit_kit/`) — NEW THIS SESSION
- `audit_prompt.md` — single-file ChatGPT audit prompt; user uploads this + target md, ChatGPT returns audit md as downloadable file
- `_audit_01..12_*.md` — all 12 ChatGPT audits (user moved/replaced the prior Sonnet audits with ChatGPT outputs)

### Specs (`docs/superpowers/`)
- `specs/2026-04-23-dalio-framework-design.md` — locked design D1-D15
- `HANDOFF_2026-04-23.md` — prior handoff (superseded by this file)
- `HANDOFF_2026-04-24.md` — THIS FILE

## Approach Pivot — Audit Tooling

**Before this session:** Sonnet audits with one round per target (re-audits deferred). Re-spawn Sonnet on REJECT.

**After this session:** ChatGPT audits via single-file kit (`chatgpt_audit_kit/audit_prompt.md`). User uploads this kit + target research md to a fresh ChatGPT chat (Plus or Pro w/ Web Browsing + Code Interpreter). ChatGPT writes the audit to a downloadable `.md` file containing EVERYTHING. User saves it to `chatgpt_audit_kit/`.

**Why pivoted:** User said tokens are too valuable for the research-heavy audit work; offload to a separate paid subscription. Calibration test on pre-patch 2.3 was DRAFTED but de-facto demonstrated by real-use 1.1 audit (ChatGPT caught 3 things Sonnet missed: F1 FRED endpoint missing api_key, F5 bare `…` vs required `[…]` elision convention, F6 stale TCMDO numbers vs current FRED).

**Layer 2 (TBD):** A second-stage ChatGPT audit-of-audit + final-writer is planned ("we want to do a second layer audit, where we give a second chatgpt, to check if the audit point are accurate, and the first audit didnt miss anything, and to write the final version of the file"). NOT BUILT YET. Will live alongside `audit_prompt.md` in `chatgpt_audit_kit/` as separate artifacts. User said "have this in mind for later, it needs an engineered set of artifacts of it's own."

**Re-spawns:** Stay local on Sonnet for now. Format compliance is harder for ChatGPT; ChatGPT-rewrite calibration deferred.

## Open Items For Next Session

### 1. CRITICAL — `research/07_inflation_currency.md` over word cap

The 1.7 Sonnet re-spawn (agent `ade534`, post-pause) wrote a 3811-word file. S2 cap is 3000.

**Fix path:** Either trim the file to ≤3000w (~25 % cut) preserving § 5 / § 7 logic, or accept overage and update S2 cap (probably no — keep cap, trim file). Re-audit via ChatGPT after trim.

### 2. Reconcile audit file location

User moved all 12 `_audit_*.md` files from `research/` to `chatgpt_audit_kit/` and dropped in fresh ChatGPT-generated audits. Current layout:
- `research/` = research reports only
- `chatgpt_audit_kit/` = audit prompt + all 12 audits

The acceptance-criteria check `C3` in `research/_acceptance_criteria.md` still expects audits at `research/_audit_<SEQ>_<slug>.md`. Either:
- (a) keep audits in `chatgpt_audit_kit/` → update `_acceptance_criteria.md` C3 path
- (b) move audits back to `research/` → keep C3 as-is

**Recommend (a):** ChatGPT audits are a different artifact class than Sonnet audits; co-locating with the prompt that produced them is correct. Update C3 path.

### 3. Process the 12 ChatGPT audits

Each ChatGPT audit needs a verdict review + patches/re-spawn dispatched per cadence. Confirmed REJECT for 1.1 (mechanical fixes mostly; see `chatgpt_audit_kit/_audit_01_economic_machine.md`). Other 11 not yet read by me.

**Next action:** Read each audit, classify by verdict (PASS / PASS-with-patches / REJECT-re-spawn), apply patches in main session for PASS-with-patches, dispatch Sonnet re-spawn for REJECT.

### 4. Pending tasks (carried from prior handoff)

- Task #37: deferred re-audit sweep — REPLACED by the ChatGPT audit pass already done
- Task #51: Wave 2 audit 2.1 — DONE via ChatGPT (`_audit_08_template_for_investing.md`)
- Task #53: 1.7 re-spawn — DONE (file landed at 3811w; trim required)

## Active Constraints / Preferences

(All carried from `memory/` files; reproduced here so resume doesn't depend on memory load.)

- **Parallelism cap: 1 background agent.** Reduced from 2 mid-session post-compaction. Floor + ceiling.
- **Agent models:** Opus for first-pass writing only. Sonnet for audit AND rewrite/re-spawn. (As of pivot, audits move to ChatGPT entirely; rewrites stay Sonnet locally.)
- **Audit cadence:** one round per target. Re-audits deferred (now de-facto served by the ChatGPT pass that just happened).
- **Repo:** PUBLIC GitHub (https://github.com/Siriko404/RayDalioAttitude). No paywalled sources, ≤1 sentence commercial-book quotes (≤2 cumulative per book).
- **Visual:** black + elegant green; Fraunces + Inter + IBM Plex Mono; 4pt grid; no drop shadows / gradients / rounded-XL.
- **Dashboard:** single HTML, pure JS, ECharts via CDN; no Pyodide / server / build step; 60s auto-refresh with pulse animation.

## Resume Protocol

1. Read this file fully.
2. `git log --oneline -8` to confirm HEAD = `05c1dea`.
3. Read `chatgpt_audit_kit/audit_prompt.md` to understand the ChatGPT workflow.
4. Read each of the 12 audits in `chatgpt_audit_kit/_audit_*.md`. Classify by verdict.
5. For 1.7 specifically: trim `research/07_inflation_currency.md` from 3811w → ≤3000w (cut narrative/§ 10 prose, preserve § 5 / § 7 logic). Trim is mechanical.
6. Apply patches from PASS-with-patches audits. Dispatch Sonnet re-spawns for REJECT.
7. After all 12 are PASS / PASS-with-patches → start the consolidation: README + `dalio_dashboard.html` + `dalio_model.xlsx`.

## Anti-Patterns (Don't Do These)

- Don't launch >1 background agent simultaneously — user has killed over-launches.
- Don't run additional Sonnet audits — audits now go through ChatGPT kit.
- Don't ask permission for routine execution (commits, single-agent dispatches per cadence). DO ask via `AskUserQuestion` for policy decisions (file layout reconciliation, cap changes, scope changes).
- Don't trust ChatGPT audit verdicts blindly — spot-check at least the Evidence column for one finding per audit before applying patches.
- Don't assume Code Interpreter has outbound network — `requests.get()` often fails. ChatGPT compensates via Web Browsing + screenshots; the audit kit explicitly allows this fallback.
