# Wave 0 — Dual Pilot (Subsections 1.1 + 2.2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce two pilot research reports — `research/01_economic_machine.md` (subsection 1.1, Economic Machine Template) and `research/09_all_weather.md` (subsection 2.2, All-Weather Portfolio) — that each pass every item in the acceptance criteria checklist. Compare them and capture lessons in a single retrospective that proposes specific prompt refinements for Wave 1.

**Why two pilots:** Per advisor review. Subsection 1.1 is foundational/narrative — hardest test of Hard Rule R4 (≤15% theory). Subsection 2.2 is naturally numeric — cleanest test. Running both in parallel lets us distinguish "prompt weakness" from "subsection resisting the schema."

**Architecture:** Stand up shared research infrastructure (deterministic prompt template with embedded subsection registry, acceptance-criteria checklist) so all 12 subsections reuse the same artifacts. Spawn **two** pilot subagents in parallel, one per subsection. Evaluate both outputs against the same checklist. Write a unified retrospective. Present to user. Iterate the template before fanning out Waves 1–3.

**Tech stack:**
- Markdown for prompt template, criteria, retrospective, reports
- Markdown table for subsection registry (inline in the prompt template file)
- `Agent` tool with `subagent_type: "general-purpose"` for research subagents
- Git + GitHub (repo live at https://github.com/Siriko404/RayDalioAttitude)
- Bash + Edit/Write for all file operations

**Non-goals for Wave 0:**
- Do NOT spawn more than two agents
- Do NOT start Wave 1 until user approves the refined prompt template
- Do NOT build any dashboard/Excel artifact yet
- Do NOT mechanize the prompt-filling step (manual substitution is fine for 2 agents)

---

## File structure (all new, all under project root)

Consolidated per advisor review: **4 files**, not 7. The subsection registry lives inside the prompt template file. The filled prompt is ephemeral (passed inline to the Agent tool). Evaluation + retrospective are one document.

| Path | Purpose | Who writes it |
|---|---|---|
| `research/_prompt_template.md` | The deterministic prompt from spec § 7 (with `{SEQ}`, `{ID}`, `{TITLE}`, `{SCOPE_IN}`, `{SCOPE_OUT}` slots) **and** the 12-row subsection registry table at the bottom. One file, one source of truth for all 12 subsections. | Me (Task 1) |
| `research/_acceptance_criteria.md` | Objective pass/fail checklist every agent report must pass | Me (Task 2) |
| `research/01_economic_machine.md` | Pilot A output (subsection 1.1) | Subagent A (Task 4) |
| `research/09_all_weather.md` | Pilot B output (subsection 2.2) | Subagent B (Task 4) |
| `research/_wave0_retro.md` | Findings (both pilots evaluated against checklist) + comparison + proposed prompt refinements for Wave 1 | Me (Task 5) |

No existing files are modified in Wave 0 except `research/_prompt_template.md` in Task 7 (refinements after user approval).

---

## Task index (7 tasks — reduced from 9 after consolidation)

1. **Create deterministic prompt template + subsection registry** — `research/_prompt_template.md`
2. **Create acceptance criteria checklist** — `research/_acceptance_criteria.md`
3. **Prepare the two filled prompts** (inline strings for the Agent tool — NOT saved as files)
4. **Spawn the two pilot subagents in parallel** — produces `research/01_economic_machine.md` and `research/09_all_weather.md`
5. **Evaluate both pilots + write retrospective** — `research/_wave0_retro.md`
6. **Present to user for review** — inline, no file
7. **Apply approved refinements to the template** — modify `research/_prompt_template.md`

---

### Task 1: Create deterministic prompt template + subsection registry

**Files:**
- Create: `research/_prompt_template.md`

- [ ] **Step 1: Write the file**

Copy the deterministic prompt from spec § 7 (`docs/superpowers/specs/2026-04-23-dalio-framework-design.md`) verbatim into the file. Keep the `{SEQ}`, `{ID}`, `{TITLE}`, `{SCOPE_IN}`, `{SCOPE_OUT}` as literal placeholder strings — do not fill them. Append the 12-row subsection registry table at the bottom by copying § 5 of the spec (the SEQ / ID / Slug / Title table and the IN/OUT bullets for each subsection).

Use the Read tool to re-read spec § 5 and § 7 first so you copy the current state (not a stale memory). Structure the file like this:

```markdown
# Deterministic Research Prompt Template

> **Purpose.** Every one of the 12 research subagents receives this exact prompt with only five slots filled: {SEQ}, {ID}, {TITLE}, {SCOPE_IN}, {SCOPE_OUT}. Values come from the Subsection Registry below.

## The Prompt (copy-paste this; fill the five slots)

<copy spec § 7 ROLE/SUBSECTION/SOURCE PRIORITY/DELIVERABLE/HARD RULES/REQUIRED OUTPUT SCHEMA/TONE block verbatim>

---

## Subsection Registry

### Mapping

| SEQ | ID | Slug | Title |
|---|---|---|---|
| 01 | 1.1 | economic_machine | Economic Machine Template |
| ... 11 more rows from spec § 5 ... |

### Scope Boundaries (IN and OUT for each subsection)

#### 1.1 Economic Machine Template
- **IN:** ...
- **OUT:** ...

#### 1.2 Short-Term Debt Cycle
- **IN:** ...
- **OUT:** ...

... 10 more entries, one per subsection ...
```

- [ ] **Step 2: Verify the file**

Run: `grep -c "^## " "research/_prompt_template.md"` → expect exactly **2** (`## The Prompt`, `## Subsection Registry`)
Run: `grep -c "^#### " "research/_prompt_template.md"` → expect exactly **12** (one per subsection)
Verify: the five placeholder strings `{SEQ}`, `{ID}`, `{TITLE}`, `{SCOPE_IN}`, `{SCOPE_OUT}` each appear at least once.

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/sinas/OneDrive/Desktop/Projects/RayDalioAttitude"
git add research/_prompt_template.md
git commit -m "feat(research): add deterministic prompt template + 12-subsection registry"
git push
```

---

### Task 2: Create acceptance criteria checklist

**Files:**
- Create: `research/_acceptance_criteria.md`

- [ ] **Step 1: Write the file**

Every line is an objective pass/fail test. No subjective "good quality" — only "section exists," "table has N columns," "word count between X and Y." Use this structure:

```markdown
# Acceptance Criteria — Research Subagent Reports

Every agent report (`research/<SEQ>_<slug>.md`) MUST pass ALL items below.
A single FAIL = report rejected and re-run (with refined prompt if needed).

## Structural criteria (format / presence)

- [ ] S1. File exists at `research/<SEQ>_<slug>.md` matching the registry.
- [ ] S2. Word count is between 2000 and 3000 (use `wc -w`).
- [ ] S3. Exactly 10 top-level sections titled `## § 1` through `## § 10`, in order.
- [ ] S4. Each section has the EXACT title prescribed by the prompt template's REQUIRED OUTPUT SCHEMA.
- [ ] S5. § 1 Executive Summary is ≤ 100 words.
- [ ] S6. § 4 Input Variables Table has columns: name | description | unit | data source | API endpoint | update frequency | typical range.
- [ ] S7. § 8 has three sub-sections: 8a JS, 8b Excel, 8c ECharts — all present.

## Content criteria (hard rules R1-R9)

- [ ] R1. Every required section (10) is present — satisfied by S3/S4.
- [ ] R2. Every numeric threshold / formula has a source citation (Dalio or non-Dalio marker).
- [ ] R3. Every input variable in § 4 has a specific public data source + API endpoint / dataset ID.
- [ ] R4. Word-count estimate: ≥85% of the text is inputs / formulas / computation / worked example / implementation; ≤15% is narrative theory. (Eyeball + word-count of § 2+§ 3 vs § 4-§ 8.)
- [ ] R5. Any ambiguity in Dalio's writing is explicitly flagged in § 10, not papered over.
- [ ] R6. No content belongs to another subsection per the registry's OUT-OF-SCOPE list.
- [ ] R7. Every claim has a visual attribution marker: either `> **Dalio** — source: ...` or `> **NON-DALIO (industry standard)** — source: ...`. No un-attributed claims.
- [ ] R8. Every cited source has a public URL. No paywalled, library-only, or login-gated links.
- [ ] R9. No commercial-book quote exceeds 1 sentence; cumulative quotes from any one commercial book ≤ 2 sentences.

## Palette-compliance criteria (§ 8c)

- [ ] P1. Every ECharts color in § 8c is one of the locked palette tokens from spec § 13 (bg-canvas, bg-surface, green-core, green-soft, green-glow, signal-red). No arbitrary colors.

## Closeout

- [ ] C1. § 9 lists at least one upstream and one downstream integration point (even if "none").
- [ ] C2. § 10 lists all sources with full citations + public URLs.
```

- [ ] **Step 2: Verify the file**

Run: `grep -c "^- \[ \]" "research/_acceptance_criteria.md"` → expect **exactly 19** checkboxes (S1–S7 + R1–R9 + P1 + C1–C2).
Manually scan: no item says "should be good" / "appropriate" / "sufficient" — every item is objective.

- [ ] **Step 3: Commit**

```bash
git add research/_acceptance_criteria.md
git commit -m "feat(research): add objective acceptance criteria checklist"
git push
```

---

### Task 3: Prepare the two filled prompts

**Files:**
- (No file written — these are inline strings for Step 2 below.)

- [ ] **Step 1: Build the two prompts by substitution**

For Pilot A (subsection 1.1):
1. Read `research/_prompt_template.md`.
2. Substitute: `{SEQ}` → `01`, `{ID}` → `1.1`, `{TITLE}` → `Economic Machine Template`.
3. Substitute `{SCOPE_IN}` with the IN bullets from the registry's 1.1 entry.
4. Substitute `{SCOPE_OUT}` with the OUT bullets from the registry's 1.1 entry.
5. Keep the final string in memory — name it `prompt_A`.

For Pilot B (subsection 2.2):
1. Substitute: `{SEQ}` → `09`, `{ID}` → `2.2`, `{TITLE}` → `All-Weather (Beta) Portfolio`.
2. Substitute scope IN/OUT from registry 2.2 entry.
3. Keep the final string in memory — name it `prompt_B`.

- [ ] **Step 2: Verify no unfilled placeholders remain in either prompt**

For each of `prompt_A` and `prompt_B`: confirm the literal strings `{SEQ}`, `{ID}`, `{TITLE}`, `{SCOPE_IN}`, `{SCOPE_OUT}` do not appear. If any remain, substitution failed — fix before spawning.

No commit in Task 3 (no file was written).

---

### Task 4: Spawn the two pilot subagents in parallel

**Files:**
- Create (by subagents): `research/01_economic_machine.md`, `research/09_all_weather.md`

- [ ] **Step 1: Spawn both agents in a single tool-call message**

Use the Agent tool. Fire both in the SAME message so they run concurrently. Use `subagent_type: "general-purpose"` for both. Give each a distinct `description`:
- Pilot A description: `"Wave 0 pilot A — Economic Machine Template"`
- Pilot B description: `"Wave 0 pilot B — All-Weather Portfolio"`

The `prompt` for Pilot A = `prompt_A` from Task 3. The `prompt` for Pilot B = `prompt_B` from Task 3.

**Critical:** append these two lines to each prompt before spawning (so the agents save their output to the right path and don't skip it):
```
FINAL STEP (required): write your full report to
  research/01_economic_machine.md    (Pilot A)
  research/09_all_weather.md         (Pilot B)
using the Write tool. Do not return your report inline — save it to the file.
```

- [ ] **Step 2: Wait for both to return**

Both agents execute. When they return, note their completion summaries but do NOT treat those summaries as truth — verify by reading the files directly.

- [ ] **Step 3: Verify both files exist**

```bash
ls -la research/01_economic_machine.md research/09_all_weather.md
wc -w research/01_economic_machine.md research/09_all_weather.md
```
Both files must exist. Word count for each should be 2000–3000.

If a file is missing or out of range: flag in retro (Task 5), do NOT re-spawn immediately — the whole point of the pilot is to learn from whatever came back.

- [ ] **Step 4: Commit both reports**

```bash
git add research/01_economic_machine.md research/09_all_weather.md
git commit -m "feat(research): add Wave 0 pilot reports (1.1 Economic Machine, 2.2 All-Weather)"
git push
```

---

### Task 5: Evaluate both pilots + write retrospective

**Files:**
- Create: `research/_wave0_retro.md`

- [ ] **Step 1: Walk each pilot through the checklist**

Open `research/_acceptance_criteria.md`. For each of the ~20+ items, mark pilot A and pilot B as PASS / FAIL / PARTIAL. Keep notes for every FAIL or PARTIAL explaining what's missing. Do this by reading each pilot file fully with the Read tool — do not rely on summary memory.

- [ ] **Step 2: Write the retrospective file**

Structure:

```markdown
# Wave 0 Retrospective

**Date:** <today>
**Pilots:** 01_economic_machine (subsection 1.1), 09_all_weather (subsection 2.2)

## 1. Checklist results

| Item | Pilot A (1.1) | Pilot B (2.2) | Notes |
|---|---|---|---|
| S1 | PASS | PASS | |
| S2 | PASS (2417 words) | PASS (2650 words) | |
| ... one row per checklist item ... |

## 2. Pattern findings (what both pilots got right / wrong)

- What worked in BOTH: <observations>
- What failed in BOTH: <observations — these are prompt problems, not subsection problems>
- Where A and B diverged: <observations — these flag subsection-specific issues>

## 3. Proposed prompt refinements for Wave 1

Each refinement is specific and testable:

1. **Refinement #1:** <exact text to add/change in `_prompt_template.md`> — addresses <which failed checklist items>.
2. **Refinement #2:** ...
3. **Refinement #3:** ...

## 4. Recommendation

- [ ] Launch Wave 1 with refinements applied
- [ ] Re-run one pilot with refinements before Wave 1 (if multiple structural failures)
- [ ] Abort Wave 0 and escalate (if fundamental prompt rethink needed)
```

- [ ] **Step 3: Verify the retrospective**

Read the file back. Every checklist row is filled for both pilots. Every proposed refinement is concrete (a diff, not a wish).

- [ ] **Step 4: Commit**

```bash
git add research/_wave0_retro.md
git commit -m "docs(research): add Wave 0 retrospective with proposed prompt refinements"
git push
```

- [ ] **Step 5: Call advisor**

This is the second (and final) advisor call for Wave 0 per the batching decision. Call the advisor tool with no parameters — the advisor sees the entire conversation and can evaluate the retro + proposed refinements on the merits.

---

### Task 6: Present to user for review

**Files:** none.

- [ ] **Step 1: Write the inline summary**

In chat, present:
1. Checklist pass rates for A and B (e.g., "A: 18/20 pass; B: 20/20 pass").
2. The 3–5 most important proposed refinements from retro § 3.
3. Which recommendation the retro makes (launch Wave 1 / re-pilot / abort).
4. Link to `research/_wave0_retro.md`.
5. Use `AskUserQuestion` with three options: approve refinements → launch Wave 1; request changes to refinements; abort and rethink.

- [ ] **Step 2: Wait for user decision**

Do not proceed to Task 7 until the user responds.

---

### Task 7: Apply approved refinements to the template

**Files:**
- Modify: `research/_prompt_template.md`

- [ ] **Step 1: Apply each approved refinement**

For every refinement the user approved in Task 6, use the Edit tool to modify `research/_prompt_template.md`. Each Edit must be surgical — change only what the refinement prescribes. Do not reorganize, do not "improve" unrelated text.

- [ ] **Step 2: Verify the template still parses**

Run the same verification as Task 1 Step 2:
- `grep -c "^## " "research/_prompt_template.md"` → still 3
- `grep -c "^#### " "research/_prompt_template.md"` → still 12
- The five slot placeholders still exist

- [ ] **Step 3: Commit**

```bash
git add research/_prompt_template.md
git commit -m "refactor(research): apply Wave 0 refinements to prompt template"
git push
```

- [ ] **Step 4: Signal readiness for Wave 1**

In chat, tell user: "Wave 0 closed. Prompt template refined and committed. Ready to launch Wave 1 (subsections 1.2, 1.3, 1.4, 1.5) on your signal."

---

## Self-review (run before declaring the plan done)

After writing the plan, check:

1. **Spec coverage.** Does this plan cover everything in the spec's § 16 Approval Checklist that's relevant to Wave 0? (Yes: § 5 subsections, § 7 prompt, § 7 output schema, § 2 public-repo. Items like § 10 dashboard / § 11 Excel / § 13 visual language are NOT Wave 0 — they come in later waves.)
2. **Placeholder scan.** Search for "TBD", "TODO", "fill in later". Replace any found with concrete content.
3. **Type / name consistency.** `prompt_A` / `prompt_B` labels match between Task 3 and Task 4. Filenames `01_economic_machine.md` and `09_all_weather.md` match between file-structure table and tasks. Yes.
4. **Ambiguity.** Every "verify" step names what to grep / wc / read. Every "commit" step has a concrete message. No "appropriately" / "sufficient" language.

If any issue found, fix inline. No second self-review.

Commit after each task that writes a file. Push after the last commit.

---
